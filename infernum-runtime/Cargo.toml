[package]
name = "infernum-runtime"
version = "0.1.0"
edition = "2021"

[features]
default = []
cuda = ["infernum/cuda"]
nccl = ["cuda", "infernum/nccl"]

[dependencies]
infernum = { path = "../infernum" }
